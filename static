<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>EXECOGIM Gene-Guided Exercise</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, sans-serif;
      background: #f4f7f9;
      color: #333;
      margin: 0;
      padding: 0;
      display: flex;
      flex-direction: column;
      align-items: center;
      min-height: 100vh;
    }
    header {
      background: #0078d4;
      color: white;
      width: 100%;
      padding: 1rem;
      text-align: center;
    }
    main {
      background: white;
      margin-top: 2rem;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 3px 8px rgba(0,0,0,0.1);
      width: 90%;
      max-width: 600px;
    }
    input, select, button {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      border-radius: 6px;
      border: 1px solid #ccc;
      font-size: 1rem;
    }
    button {
      background: #0078d4;
      color: white;
      cursor: pointer;
    }
    button:hover {
      background: #005fa3;
    }
    #result {
      margin-top: 1.5rem;
      padding: 1rem;
      background: #e9f5ff;
      border-radius: 8px;
      display: none;
    }
  </style>
</head>
<body>
  <header>
    <h1>EXECOGIM</h1>
    <p>Malaysia’s First Gene-Guided Exercise Prescription System</p>
  </header>

  <main>
    <h2>Personalized Exercise Plan Generator 🧬</h2>
    <form id="geneForm">
      <label>BDNF Genotype:</label>
      <select id="genotype" required>
        <option value="Val/Val">Val/Val</option>
        <option value="Met/Met">Met/Met</option>
        <option value="Val/Met">Val/Met</option>
      </select>

      <label>Age:</label>
      <input type="number" id="age" value="40" min="18" required />

      <label>Fitness Level:</label>
      <select id="fitness">
        <option value="beginner">Beginner</option>
        <option value="moderate" selected>Moderate</option>
        <option value="advanced">Advanced</option>
      </select>

      <button type="submit">Generate Plan</button>
    </form>

    <div id="result"></div>
  </main>

  <script>
    const form = document.getElementById('geneForm');
    const resultDiv = document.getElementById('result');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      resultDiv.style.display = 'none';
      resultDiv.innerHTML = 'Generating plan... ⏳';

      const data = {
        genotype: document.getElementById('genotype').value,
        age: document.getElementById('age').value,
        fitness_level: document.getElementById('fitness').value
      };

      try {
        const res = await fetch('/api/generate_plan', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });
        const plan = await res.json();

        resultDiv.style.display = 'block';
        resultDiv.innerHTML = `
          <h3>✅ Personalized Plan Generated</h3>
          <p><b>Genotype:</b> ${plan.genotype_label}</p>
          <p><b>Sessions per week:</b> ${plan.summary.sessions_per_week}</p>
          <p><b>Session length:</b> ${plan.summary.session_length_min} min</p>
          <p><b>Intensity:</b> ${plan.summary.intensity}</p>
          <p><b>Generated at:</b> ${new Date(plan.generated_at).toLocaleString()}</p>
        `;
      } catch (err) {
        resultDiv.style.display = 'block';
        resultDiv.innerHTML = `<p style="color:red;">❌ Failed to generate plan. Check API or network connection.</p>`;
      }
    });
  </script>
</body>
</html>

